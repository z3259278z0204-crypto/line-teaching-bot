# LINE 教具記錄機器人 — 部署說明

## 第一步：把程式碼推上 GitHub

1. 到 github.com 新增一個 Repository（例如：`line-teaching-bot`）
2. 在終端機執行：

```bash
cd ~/Downloads/承恩-agent/line-teaching-bot
git init
git add .
git commit -m "init"
git remote add origin https://github.com/你的帳號/line-teaching-bot.git
git push -u origin main
```

---

## 第二步：在 Render 部署

1. 到 **render.com** 登入（可用 GitHub 帳號）
2. 點右上角「New +」→「Web Service」
3. 選剛才建立的 GitHub Repo
4. 設定如下：
   - **Name**：line-teaching-bot
   - **Runtime**：Python 3
   - **Build Command**：`pip install -r requirements.txt`
   - **Start Command**：`gunicorn main:app`
5. 按「Create Web Service」，等待部署完成
6. 部署成功後，你會得到一個網址，例如：`https://line-teaching-bot.onrender.com`

---

## 第三步：設定環境變數

在 Render 的「Environment」頁籤新增三個變數：

| 變數名稱 | 值 |
|---|---|
| `LINE_CHANNEL_ACCESS_TOKEN` | 從 LINE Developers 複製 |
| `LINE_CHANNEL_SECRET` | 從 LINE Developers 複製 |
| `BASE_URL` | `https://line-teaching-bot.onrender.com`（你的Render網址）|

---

## 第四步：設定 LINE Webhook

1. 到 **developers.line.biz** → 你的 Channel → Messaging API
2. 找到「Webhook URL」，填入：
   ```
   https://line-teaching-bot.onrender.com/webhook
   ```
3. 開啟「Use webhook」
4. 按「Verify」驗證是否成功（顯示 Success 即可）

---

## 使用方法

| 你說 | 機器人回應 |
|---|---|
| 記錄教具 | 開始逐步詢問週次→教具→目標 |
| 查看記錄 | 顯示最近5筆記錄 |
| 匯出Excel | 傳送10分鐘有效下載連結 |
| 刪除最後一筆 | 刪除最新那筆記錄 |
| 取消 | 中斷目前輸入流程 |
| 說明 | 顯示所有功能 |

---

## 注意事項

- **免費版 Render**：閒置15分鐘會進入休眠，收到訊息時需30-60秒喚醒
- **資料存放**：SQLite 存在 `/tmp`，Render 重新部署時資料會清除
  → 建議每週定期說「匯出Excel」備份
- 若要永久保留資料，之後可升級加入 Render Disk（$1/月）
